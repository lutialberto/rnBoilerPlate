import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const TestPrettier = () => {
  return (
    <View>
      <Text>TestPrettier</Text>
    </View>
  );
};

export default TestPrettier;

const styles = StyleSheet.create({});
